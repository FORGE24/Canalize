#include "canalize/block/BlockState.h"

namespace Canalize {
    namespace Block {
        const BlockState Blocks::AIR(0);
        const BlockState Blocks::STONE(1);
        const BlockState Blocks::GRASS(2);
        const BlockState Blocks::DIRT(3);
        const BlockState Blocks::WATER(9);
    }
}
